#!/usr/bin/node
const stringsToPrint = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let arrayIdx = 0;
while (arrayIdx < stringsToPrint.length) {
  console.log(stringsToPrint[arrayIdx]);
  arrayIdx++;
}
